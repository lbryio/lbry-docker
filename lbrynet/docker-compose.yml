version: '3.4'

services:
#############
## Lbrynet ##
#############
  lbrynet:
    build: .
    ports:
      - 5280:52800
      - 4444:4444
      - 50001:50001
    ## host volumes for persistent data such as wallet private keys.
    volumes:
      - ./data:/lbrynet
